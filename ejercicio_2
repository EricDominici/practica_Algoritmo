
/*2. Haga un programa al cual se le digite un número entero
 y despliegue por pantalla la suma d e  los números comprendidos 
 entre 0 y el número digitado. */
// Leer numero 

// Leer numero 
let num = 9;
let sum = 0;



// Gauss
num = 9;
let sum_gauss = num * (num + 1)/2
console.log(sum_gauss);